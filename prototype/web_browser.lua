local lgi = require 'lgi'
local gtk = lgi.Gtk
local gdk = lgi.Gdk
local webkit = lgi.WebKit2

local utils = require 'utils'
local class = utils.class

local WebBrowser = class()

-- "https://gitlab.gnome.org/World/eolie"

return WebBrowser

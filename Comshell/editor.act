use .gi.repository.Glib as glib
use .gi.repository.Gio as gio
use .gi.repository.Gdk as gdk
use .gi.repository.Gtk as gtk
use .gi.repository.GtkSource as geditor

class 'editor
  buffer: geditor.'buffer
  view: geditor.'view

  fn init() =>
    self.buffer = geditor.'buffer()
    self.view = geditor.'view()

  fn prev-page() =>

  fn next-page() =>

  fn prev-item() =>

  fn next-item() =>

# gspell

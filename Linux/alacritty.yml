colors:
  primary:
    background: '0x222222'
    foreground: '0xeeeeee'
font:
  size: 11.5
key_bindings:
  - { key: T, mods: Control, command:
      { program: "sh", args: ["-c",
        "while true; do alacritty || break; pgrep -x alacritty >/dev/null && break; done"] } }

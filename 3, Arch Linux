Arch Linux helps us to setup and maintain a Linux system;
first boot to an Arch Linux live image;
live image can be useful in the future for repairing a damaged system; so keep it;

to ensure the system clock is accurate:
; timedatectl set-ntp true

list available block devices using "lsblk", then use "parted" to create the partitions:
; parted /dev/<device> mklabel gpt mkpart P1 fat32 0% 260MiB mkpart P2 260MiB 100% set 1 esp on
; mkfs.fat -F32 /dev/<partition1>; mkfs.ext4 /dev/<partition2>
; mount /dev/<partition2> /mnt

; pacstrap /mnt refind-efi intel-ucode amd-ucode \
  base dracut linux linux-firmware e2fsprogs systemd-sysvcompat pacman \
  networkmanager pulseaudio-alsa udisks2 man-db nano unzip \
  gdm materia-gtk-theme ttf-hack noto-fonts noto-fonts-emoji \
  gtksourceview4 webkit2gtk gst-plugins-{base,good,bad} gst-libav gnome-desktop

; arch-chroot /mnt

; refind-install --usedefault /dev/<partition1>
find UUID of ESP:
; blkid /dev/<partition1>
automatically update Grub every time "grub" package is upgraded:
; mkdir -p /etc/pacman.d/hooks
; nano /etc/pacman.d/hooks/100-refind-efi.hook
  [Trigger]
  Type = Package
  Operation = Upgrade
  Target = refind-efi
  [Action]
  Description = Updating refind-efi
  When = PostTransaction
  Exec = /usr/bin/refind-install --usedefault /dev/disk/by-uuid/<ESP-UUID>

; nano /etc/locale.gen
  uncomment "en_US.UTF-8 UTF-8"
; locale-gen
; echo -e "LANG=en_US.UTF-8" > /etc/locale.conf

; systemctl enable systemd-timesyncd.service
; systemctl enable NetworkManager.service

"https://www.techrapid.uk/2017/04/automatically-update-arch-linux-with-systemd.html"
"https://wiki.archlinux.org/index.php/Systemd/Timers"

; useradd -m -G wheel user1
; passwd user1
; passwd

; systemctl enable gdm.service
automatic login:
; nano /etc/gdm/custom.conf
  [daemon]
  AutomaticLogin=user1
  AutomaticLoginEnable=True
actually, to protect the computer from physical attacks, you have to disable automatic login,
  and lock the session when you leave the computer;
in addition you have to:
, somehow prevent tampering with hardware;
, disable boot from USB (and other external ports);
, protect boot firmware by a password;
these can make physical attacks more difficult, but not impossible;
physical access to a computer is root access, given enough time and resources;

; mkdir -p /usr/local/share/gnome-shell/extensions
; cp -r etc/gnome-shell-reduced/ /usr/local/share/gnome-shell/extensions/

; exit
; reboot

; gsettings set org.gnome.desktop.screensaver lock-enabled 'false'

; mkdir -p ~/.config/fontconfig
; cp etc/fonts.conf ~/.config/fontconfig/

; mkdir -p ~/.config/gtk-3.0
; gsettings set org.gnome.desktop.interface gtk-theme Materia-light-compact
; gsettings set org.gnome.desktop.interface font-name Sans
; cp etc/gtk.css ~/.config/gtk-3.0/

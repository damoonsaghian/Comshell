implementing a complete GUI toolkit is a huge lot of work;
existing ones (like GTK) are single threaded, thus we can't create actors from them;
so for data structures containing widgets:
, use "RefCell", where you want mutability;
, use "Rc" to create references;

"https://mmstick.github.io/gtkrs-tutorials/introduction.html"
"https://github.com/gtk-rs/examples/tree/master/src/bin" (list_store ...)
"https://developer.gnome.org/platform-overview/unstable/"
"https://gtk-rs.org/docs/gtk/"
"https://gtk-rs.org/docs/sourceview/"
"https://gtk-rs.org/docs/gtk/trait.TextBufferExt.html#tymethod.get_text"

"https://stackoverflow.com/questions/23433819/creating-a-simple-file-browser-using-python-and-gtktreeview"
"https://github.com/teejee2008/polo/tree/master/src/Gtk"
"https://github.com/teejee2008/polo/tree/master/src/Utility"
"https://github.com/fengalin/media-toc"
"https://developer.gnome.org/SaveWindowState/"

; sudo pacman -S sway xorg-server-xwayland materia-gtk-theme
; sudo pacman -S gtk3 gtksourceview3 webkit2gtk gst-plugins-{base,good,bad} gst-libav

; nano ~/.bash_profile
  export TZ="/usr/share/zoneinfo/<your_location>"
  sway
; mkdir -p ~/.config/sway
; nano ~/.config/sway/config
  default_border none
  set $gnome-schema org.gnome.desktop.interface
  exec_always {
    gsettings set $gnome-schema gtk-theme Materia-light-compact
    gsettings set $gnome-schema font-name Sans
  }
  exec --no-startup-id "cargo run --release --manifest-path path-to-Cargo.toml; swaymsg exit"

; mkdir -p ~/.config/gtk-3.0
; cp gtk.css ~/.config/gtk-3.0/gtk.css
